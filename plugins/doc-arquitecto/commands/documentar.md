# /documentar — Producir la documentación-contrato del proyecto

> ⚠️ **Esqueleto (S01)** — este comando aún NO está implementado. Su implementación llega en S02 (modo nuevo: entrevista guiada) y S03 (modo existente: huecos + diffs confirmados). Ver `docs/FICHA.md` del repo del plugin.

Contexto adicional del usuario (si lo hay): $ARGUMENTS

## Qué hacer por ahora

Informale al usuario, en una respuesta breve:

1. Que `/documentar` está instalado correctamente pero es un **placeholder de la sesión S01 (Esqueleto)**.
2. Qué va a hacer cuando esté implementado: en un repo sin docs, entrevista guiada (propósito → alcance → decisiones estructurales con opciones y tradeoffs → plan de sesiones) y generación de `docs/` (VISION, ALCANCE, PLAN, decisiones/, references/, business/); en un repo con docs, detección de huecos y completado con diffs confirmados, sin pisar nada.
3. Que NO intente suplir la funcionalidad improvisando: no generes documentación en su lugar. La regla de la herramienta es que lo estructural lo decide el humano — y esa entrevista todavía no existe.

No hagas nada más. No escribas archivos. No inventes la funcionalidad.
