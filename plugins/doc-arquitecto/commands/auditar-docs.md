# /auditar-docs — Auditar la documentación como contrato

> ⚠️ **Esqueleto (S01)** — este comando aún NO está implementado. Su implementación llega en S04 (las seis dimensiones de auditoría + informe accionable + arreglos confirmados). Ver `docs/FICHA.md` del repo del plugin.

Contexto adicional del usuario (si lo hay): $ARGUMENTS

## Qué hacer por ahora

Informale al usuario, en una respuesta breve:

1. Que `/auditar-docs` está instalado correctamente pero es un **placeholder de la sesión S01 (Esqueleto)**.
2. Qué va a hacer cuando esté implementado: auditar el plano mismo en seis dimensiones — completitud (¿cada sesión tiene ≥1 criterio verificable?), contradicciones entre documentos, criterios no verificables, decisiones estructurales sin registrar, referencias faltantes o vencidas (frescura fechada) y drift interno doc↔doc — con informe `file:línea` y arreglos aplicados uno por uno con confirmación.
3. Que NO intente suplir la funcionalidad improvisando una auditoría parcial en su lugar.

No hagas nada más. No escribas archivos. No inventes la funcionalidad.
